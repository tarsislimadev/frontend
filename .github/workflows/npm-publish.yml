name: Node.js Package

on:
  workflow_dispatch:

jobs:
  publish-npm:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
      - run: npm login --scope=@brtmvdl --registry=https://registry.mycorp.com
      - run: npm publish --provenance --access public --registry=https://registry.mycorp.com
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
